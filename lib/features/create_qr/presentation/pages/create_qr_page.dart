import 'package:flutter/material.dart';

class CreateQrPage extends StatelessWidget {
  const CreateQrPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Create QR")));
  }
}
